---
- name: install debian apt contrib debian repo
  apt_repository: >
    repo="deb http://httpredir.debian.org/debian {{ ansible_lsb.codename }} contrib" 
  become: yes
  when:
    ansible_distribution == "Debian"
