---
- name: install docker yum repo
  copy: >
    src=docker.repo
    dest=/etc/yum.repos.d
  become: yes

- name: install docker rpm package
  yum: >
    name={{ item }} 
    state=latest
  with_items:
    - docker-engine
  become: yes
