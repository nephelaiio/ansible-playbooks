---
openjdk_version: 8

openjdk_jdk: no

openjdk_jdk_apt_packages:
  - "{{ 'openjdk-' ~ openjdk_version ~ ('-jdk' if openjdk_jdk else '-jre') }}"

openjdk_jdk_yum_packages:
  - "{{ 'java-1.' ~ openjdk_version ~ '.0-openjdk' ~ ('-devel' if openjdk_jdk else '') }}"
