---
openjdk_jdk_packages:
  - "{{ 'java-1.' ~ openjdk_version ~ '.0-openjdk' ~ '-devel' ~ ('-headless' if openjdk_headless else '') }}"
 
openjdk_jre_packages:
  - "{{ 'java-1.' ~ openjdk_version ~ '.0-openjdk' ~ ('-headless' if openjdk_headless else '') }}"
