---
openjdk_jdk_packages:
  - "{{ 'openjdk-' ~ openjdk_version ~ '-jdk' ~ ('-headless' if openjdk_headless  else '') }}"

openjdk_jre_packages:
  - "{{ 'openjdk-' ~ openjdk_version ~ '-jre' ~ ('-headless' if openjdk_headless  else '') }}"
