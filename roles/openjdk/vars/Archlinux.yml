---
openjdk_jdk_packages:
  - "{{ 'jdk' ~ openjdk_version ~ '-openjdk' ~ ('-headless' if openjdk_headless else '') }}"

openjdk_jre_packages:
  - "{{ 'jre' ~ openjdk_version ~ '-openjdk' ~ ('-headless' if openjdk_headless else '') }}"
