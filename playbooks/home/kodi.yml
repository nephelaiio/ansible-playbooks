---
- hosts: kodi

  become: true

  tasks:

    - name: install kodi packages
      apt:
        name: "{{ item }}"
        state: present
        update_cache: yes
      with_items:
        - kodi

    - name: start kodi services
      service:
        name: kodi
        enabled: yes
        state: running

