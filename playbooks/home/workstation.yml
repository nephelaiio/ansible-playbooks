---
- name: Configure workstation

  hosts: cerberus

  roles:

    - nephelaiio.tree
    - nephelaiio.vim
    - nephelaiio.devtools
    - nephelaiio.rbenv
    - nephelaiio.pyenv
    - nephelaiio.tmux
    - nephelaiio.vagrant
    - nephelaiio.i3
    - nephelaiio.docker

  tasks:

    - include: ../upgrade.yml

    - include: ../kvm.yml
