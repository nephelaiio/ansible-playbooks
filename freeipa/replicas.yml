---
- name: Configure ipa replicas

  hosts: ipareplicas

  become: true

  roles:

    - bertvv.hosts
    - ipareplica

  tasks:

    - name: work around ipareplica dns forwarding errors before reboot
      reboot:
