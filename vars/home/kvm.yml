---
kvm_nolog: no
kvm_images:
  stretch:
    src: http://cdimage.debian.org/cdimage/openstack/current-9/debian-9-openstack-amd64.qcow2
    url: "{{ archive_url }}/kvm/debian-9-openstack-amd64-disk1.qcow2"
    pool: default
    format: qcow2
  xenial:
    src: https://cloud-images.ubuntu.com/xenial/current/xenial-server-cloudimg-amd64-disk1.img
    url: "{{ archive_url }}/kvm/xenial-server-cloudimg-amd64-disk1.img"
    pool: default
    format: qcow2
  centos:
    src: https://cloud.centos.org/centos/7/images/CentOS-7-x86_64-GenericCloud.qcow2.xz
    url: "{{ archive_url }}/kvm/CentOS-7-x86_64-GenericCloud.qcow2.xz"
    pool: default
    format: qcow2

kvm_profiles:
  default:
    state: present  # present | recreate | absent
    memory: 2048
    cpu: 2
    image: xenial
    autostart: True
    size: 10G
    pool: default
    nic:
      method: dhcp
      name: enp0s3
      type: bridge
      net: br1
      prefix: "{{ nets.kvm.netmask }}"
      gateway: "{{ nets.kvm.router }}"
      nameservers: "{{ nets.kvm.ns }}"
      namesearch: "{{ dns.search_domains }}"

kvm_pools:
  default:
    path: /var/lib/libvirt/images
    state: present
    autostart: True
