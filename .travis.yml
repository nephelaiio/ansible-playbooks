---
language: python

sudo: required

services:
  - docker

install:
  - sudo apt-get install -y libvirt libvirt-dev
  - pip install -r requirements.txt

script:
  - yamllint ./ -c ./.flake8
  - flake8 ./
