---
language: python

sudo: required

install:
  - sudo apt-get update
  - sudo apt-get install -y libvirt-dev
  - pip install -r requirements.txt

script:
  - yamllint ./ -c ./.yamllint
  - flake8 ./
  - pytest
