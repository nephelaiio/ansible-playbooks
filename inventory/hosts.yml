raspberry_a:
  hosts:
    192.168.0.10:
      ansible_connection: ssh
      ansible_user: pi

raspberry:
  children:
    raspberry_a:

unifi:
 children:
   raspberry_a:

dhcp:
  children:
    unifi:

local:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/env python2
