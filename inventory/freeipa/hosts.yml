---
ipaservers:
  hosts:
    kvmd.home.nephelai.io:

ipareplicas:
  hosts:
    kvme.home.nephelai.io:

ipaclients:
  children:
    ipaservers:
    ipareplicas:

freeipa:
  children:
    ipaservers:
    ipareplicas:
