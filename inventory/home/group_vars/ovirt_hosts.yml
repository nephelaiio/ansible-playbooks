---
ovirt_pvs:
  - /dev/sda2
ovirt_register_host: no
ovirt_host_network_iface: eno1
ovirt_host_networks:
  - name: ovirt
    networks:
      - name: ovirt
    clusters:
      - name: "{{ ovirt_cluster }}"
        required: yes
