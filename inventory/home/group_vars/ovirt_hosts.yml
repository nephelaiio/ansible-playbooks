---
ovirt_pvs:
  - /dev/sda2
ovirt_host_network_iface: eno1
ovirt_logical_networks:
  - name: ovirt
    data_center: "{{ ovirt_data_center }}"
    vlan_tag: 3
    vm_network: yes
    wait: yes
ovirt_host_networks:
  - name: ovirt
    networks:
      - name: "{{ ovirt_logical_networks }}"
