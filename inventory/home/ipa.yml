---
ipa_server:
  hosts:
    kvmd.home.nephelai.io:

ipa_replica:
  hosts:
    kvme.home.nephelai.io:

ipa_backend:
  vars:
    kvm_state: absent
  children:
    ipa_server:
    ipa_replica:

ipa_user:
  children:
    online:

ipa_client:
  children:
    kvm_guests:

ipa:
  children:
    ipa_server:
    ipa_replica:
    ipa_user:
