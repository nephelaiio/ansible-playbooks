---
all:
  hosts:
    kvmu.home.nephelai.io:
    kvmp.home.nephelai.io:

awx:
  children:
    awx_app:
    awx_proxy:

awx_app:
  hosts:
    kvmu.home.nephelai.io:

awx_proxy:
  hosts:
    kvmp.home.nephelai.io:
