---
- hosts: localhost
  roles:
    - localhost
    - git
    - tree
    - { role: tmux, users: ["{{ ansible_ssh_user }}"] }
    - virtualbox
    - vagrant
    - docker.core
    - { role: docker.tools, users: ["{{ ansible_ssh_user }}"] }

