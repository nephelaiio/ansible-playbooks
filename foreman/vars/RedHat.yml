---
foreman_passenger_packages:
  - nginx
  - passenger
foreman_tftp_packages:
  - xinetd
  - tftp-server
  - tftp
  - syslinux-tftpboot
foreman_nginx_user: nginx
foreman_db_packages:
  - MariaDB-server
  - MariaDB-client
foreman_app_packages:
  - foreman
  - foreman-postgresql
foreman_packages: "{{ foreman_app_packages + foreman_db_packages }}"
